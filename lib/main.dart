import 'package:flutter/material.dart';
import 'package:msufcu_flutter_project/login_page.dart';

/// Main file that is configured to open the app on the login page by default
void main() {
  runApp(const LoginPage());
}
